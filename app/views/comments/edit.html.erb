<p><%= @comment.body %></p>

<%= form_for [@result, @comment] do |f| %>

  <%= f.hidden_field :name, :value => current_user.user_name %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit %>

<% end %>
